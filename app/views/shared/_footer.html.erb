<!-- Log Out User/Admin -->
<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-bottom">
  <ul class="navbar-nav mr-auto">
    <% if user_signed_in? %>
      <% if current_user.admin? %>
        <li class="nav-item btn-group dropup">
          <button type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Welcome <%= current_user.user_name %></button>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
            <a class="dropdown-item" href="#!">Dasboard</a>
            <a class="dropdown-item" href="#!">Mesages</a>
            <%= link_to "Log out", destroy_user_session_path, :method => :delete, class: 'dropdown-item'  %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Log out", destroy_user_session_path, :method => :delete, class: 'nav-link'  %>
        </li>
      <% end %>
    <% else %>
      <li class="nav-item">
        <%= link_to "Log In", user_session_path, class: 'nav-link'  %>
      </li>
      <li class="nav-item">
        <%= link_to "Sign Up", new_user_registration_path, class: 'nav-link' %>
      </li>
    <% end %>
  </ul>

  <!-- Add Product Btn -->
  <% if user_signed_in? && current_user.admin? %>
    <%= link_to new_product_path do %>
      <button class="mdc-fab mdc-fab--extended">
        <div class="mdc-fab__ripple"></div>
        <span class="material-icons mdc-fab__icon">add</span>
        <span class="mdc-fab__label">Products</span>
      </button>
    <% end %>
  <% end %>
</nav>
<!-- Log Out User/Admin -->
